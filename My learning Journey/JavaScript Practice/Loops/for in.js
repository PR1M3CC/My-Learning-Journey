let langs = {
    HTML: "Hyper Text Markup Language",
    CSS: "Cascading Style Sheets",
    JS: "JavaScript"
}
for (const key in langs) {
console.log(`${key} is short for ${langs[key]}`);
}

let myArr = ["JS", "Ruby", "Swift", "CPP"]
for (const key in myArr) {
console.log(myArr[key]);
}